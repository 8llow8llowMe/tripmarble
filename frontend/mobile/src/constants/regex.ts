const REGEX = {
  EMAIL: /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/,
  //한글/영문/숫자 10자 이내
  NAME: /^[가-힣a-zA-Z0-9]{1,10}$/,
  NICKNAME: /^[가-힣a-zA-Z0-9]{1,10}$/,
  //영문/숫자/특수문자 포함 8~20자
  PASSWORD: /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[!@#$%^&*()_+{}[\]:;"'<>,.?/~`]).{8,20}$/,
  //http || https
  URL_REGEX: /(https?:\/\/[^\s/$.?#].[^\s]*)/gi,
};

export { REGEX };
